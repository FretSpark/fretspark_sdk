name: fretspark_sdk_example
description: Minimal example app demonstrating the FretSpark SDK.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  fretspark_sdk:
    path: ../

flutter:
  uses-material-design: true
